import React from "react"


const ProcessContainer = ({children}) => {
    return (
        <ProcessContainer>
            {children}
        </ProcessContainer>
    )
}

export default ProcessContainer